<% if object.errors.any? %>
  <div class="alert alert-danger">
    <p>入力されたフォームにエラーがあります。</p>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
